// ignore_for_file: prefer_const_constructors

import 'package:app_models/app_models.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('AppModels', () {
    test('can be instantiated', () {
      expect(AppModels(), isNotNull);
    });
  });
}
