import 'package:flutter/material.dart';

class RestaurantReviewPage extends StatelessWidget {
  const RestaurantReviewPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [
          Text('RestaurantReviewPage'),
        ],
      ),
    );
  }
}
