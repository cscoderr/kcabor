import 'package:flutter/material.dart';

class RestaurantTrackingDetailsPage extends StatelessWidget {
  const RestaurantTrackingDetailsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
