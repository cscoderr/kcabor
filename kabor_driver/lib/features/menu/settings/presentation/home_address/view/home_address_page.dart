import 'package:flutter/material.dart';

class HomeAddressPage extends StatelessWidget {
  const HomeAddressPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: const [
          Text('HomeAddressPage'),
        ],
      ),
    );
  }
}
